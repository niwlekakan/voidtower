// Phase 2: Security scanning and audit
// Checks: file permissions, exposed services, login attempts,
//         certificate expiry, dangerous capabilities
// Every finding includes: what, why, recommended fix, auto-fix availability
