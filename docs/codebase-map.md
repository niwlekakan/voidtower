# VoidTower Codebase Map

> **STALE, 2026-07-11 — corrections before trusting anything below:**
> 1. **P0-04 (irreversibility denylist) is now merged** (PR #14, `2e32747`) — every "not merged" / "still does not exist" claim about `backend/src/voidwatch/denylist.rs` below is outdated. Item 10 (last-remaining-snapshot ambiguity) was resolved by the operator: `api/proxmox.rs`'s `vm_delete_snapshot` is gated (reclassified `Irreversible`); `api/backups.rs`'s schedule-row deletion is not, since it destroys no data.
> 2. **The "62 uncommitted files" / whole-crate rustfmt-cascade artifact described below was local, uncommitted state in one specific sandbox clone (`~/.devteam-sandbox/voidtower`'s working tree), never part of `main`'s actual tracked history.** It has since been cleaned. Its root cause — `devteam.sh` never reset the working tree between tasks in the same batch, so one worker's leftover uncommitted changes silently became the next worker's starting state — is now fixed (`reset_worktree_for_task`, added 2026-07-11), so this class of contamination should not recur. Treat every claim below that reads as "X is uncommitted on main's working tree" as a description of that transient local artifact, not of `main`'s git history.
> 3. **P0 is now fully complete** — P0-01 through P0-06 are all merged (P0-03's frontend/approvals-queue half deferred to [issue #11](https://github.com/niwlekakan/voidtower/issues/11)). A full regeneration via the `doc-updater` agent is recommended at P1 kickoff to fold all of this in properly; this note is a stopgap, not a substitute.

Regenerated by a read-only survey of this repo at `main` HEAD `d3cbd86` (2026-07-11), cross-checked against source. **`main`'s tracked history advanced by one real merge since the prior map's commit (`c26da56`): PR #13, `devteam/P0-06-scope-token-and-bearer-auth-bypass`.** This is the first source change across the last three regenerations — everything else below in §1–§6 was re-verified directly and, apart from the P0-06 sections called out explicitly, matches the prior map with no drift found. This map is descriptive, not authoritative — verify behavior in source before relying on it for policy/auth/schema work. All paths are relative to the repo root unless stated otherwise.

**What changed in this cycle: P0-06 (bearer-token scope enforcement) merged, closing the scope-bypass gap the prior three maps tracked as open.** Everything else — P0-03 mode ladder + risk classes, P0.5 secrets redaction, tiered accounts, fleet node enrollment, member self-hosting hub, Tauri desktop wrapper, Expo mobile app — is unchanged from the prior map and still exactly as described in §1–§6 below.

- **P0-06 landed as three commits** (`f263b66` fix + capability tiers, `f595e3a` scoped rustfmt, `14b0da3` a same-day follow-up restoring the hand-aligned route table and adding `#[rustfmt::skip]` to `router()` — see §6), merged via `7fa1b77`. The PR's first attempt was the one the prior maps described as rejected for an unscoped `apps.rs` reformat cascading into forbidden zones; this landed version is a clean rewrite scoped to ADR-003's granted paths (`backend/src/auth/mod.rs`, new `backend/src/auth/scope_enforce.rs`, `backend/src/api/bearer_auth.rs`, `backend/src/api/integrations.rs`, `backend/src/main.rs`) plus a new `backend/src/api/scope_bypass_tests.rs` and doc updates (`docs/api-tokens.md`, `docs/integrations/mcp-server.md`). Full details in the rewritten "scope-bypass gap" entry in §4, the new `auth::scope_enforce` module in §1, and the middleware-layering update in §6.
- **P0-04 (irreversibility denylist)** remains **written and pushed but not merged** — `backend/src/voidwatch/denylist.rs` still does not exist in `main` (re-verified: `find backend/src/voidwatch` still lists only `mod.rs`, `allowlist_seed.rs`, `mode.rs`, `risk_class.rs`). Its escalation (item #10 of 12, the genuine ADR-004 textual ambiguity over "last remaining snapshot/backup") is still open, unanswered, in `.devteam/blocked/P0-04-irreversibility-denylist.md` / `.devteam/escalations/P0-04-irreversibility-denylist.md`.
- **Local queue bookkeeping remains noisier than a clean snapshot and should not be trusted from this document:** `.devteam/blocked/` has a `P0-03-mode-ladder-risk-classes.md` entry despite P0-03 already being merged (its planner note is dated 2026-07-10 and reads as a re-seeded/duplicate queue entry, not a regression), and `.devteam/failed/` has `P0-01-voidwatch-choke-point.md` and `P0-05-secrets-redaction-ai-context.md` entries even though both are long since merged and separately listed in `.devteam/done/`. `.devteam/queue/` and `.devteam/active/` are both empty. **`.devteam/` is fully gitignored** (`.gitignore:84`) — machine-local runner state, not part of the tracked tree this map surveys — re-check it directly (`.devteam/queue/`, `.devteam/active/`, `.devteam/blocked/`, `.devteam/failed/`, `.devteam/done/`, `git branch --all`) rather than trust this snapshot.

**The whole-crate rustfmt-cascade defect flagged in prior maps is still sitting uncommitted directly on `main`'s working tree, unchanged in scope, and is now formally understood at the root-cause level.** `git status` shows the same **62** files with uncommitted modifications as the prior map. This regeneration confirmed file-by-file (not spot-checked) that running `rustfmt --edition 2021` on each file's clean `HEAD` copy reproduces the working-tree version byte-for-byte — the diff is purely cosmetic, zero semantic content. **P0-06's `14b0da3` commit explains why this keeps recurring**: `rustfmt`, invoked on `api/mod.rs` (or transitively on `main.rs`, the crate root), treats any file reachable via `mod` declarations as part of one formatting pass and silently reflows all of them — so any task whose diff touches `api/mod.rs` and whose workflow runs a bare `cargo fmt`/`cargo fmt --all` (instead of formatting only its own changed files, per `CLAUDE.md`'s "Formatting is diff-scoped" rule) drags in every other module, including forbidden-zone files (`backend/src/oidc.rs`, `backend/src/api/auth.rs`, `backend/src/api/policy.rs`, all still in this diff). `14b0da3` added `#[rustfmt::skip]` to `router()` specifically to stop that file's route table from being part of the collateral, but the defense only helps once a task's diff is scoped correctly in the first place — it does not by itself prevent a `cargo fmt --all` invocation from reformatting everything else. `git stash list` still shows five stashes from prior sessions attempting to park this exact reformat off of `main`, unresolved at the root cause. **This map's regeneration did not touch, stage, or commit any of those 62 files.**

---

## 1. Backend module inventory

Modules declared in `backend/src/main.rs:1-22` plus `backend/src/api/mod.rs:32-83`.

| Module | Purpose (verified in source) |
|---|---|
| `backend/src/main.rs` | Binary entry point: CLI (clap) with `user`/`backup` subcommands, `AppState` definition, DB init, background loops (metrics collector, alert thresholds, status-check scheduler, automation scheduler, restore-test scheduler, session cleanup, Proxmox VM monitor, and — new — a 5-minute member storage/drive usage poll, `main.rs:513-521`, calling `api::members::poll_member_storage`), router mount, static frontend serving. |
| `backend/src/ai/mod.rs` | Multi-provider AI abstraction: `AiProvider` trait (`complete`/`stream`/`health_check`), `ProviderConfig` persisted in `ai_providers` table, re-exports `AiOrchestrator` and `build_provider`. Submodules `providers/` (odysseus, openai, anthropic, local) and `orchestrator.rs`/`router.rs`. |
| `backend/src/alerts/mod.rs` | Stub only — comment-only placeholder describing planned `evaluate_alerts`/`acknowledge_alert` functions. **Actual alert logic lives in `backend/src/api/alerts.rs`**, not here. |
| `backend/src/audit/mod.rs` | Real, shared audit-log library — `AuditEntry` struct, `log()`/`log_sourced()` (the latter adds a `source` tag e.g. `"odysseus"`) which every mutating handler across the codebase calls to insert into `audit_log`, plus `list()` used by `api/audit.rs`. **Not a stub** — omitted from the prior version of this map's inventory table entirely; that was an error, not a recent addition (the file predates the prior map). Distinct from `api/audit.rs`, the route handler that calls into this module (see §2). |
| `backend/src/auth/mod.rs` | Real auth core: `User`/`Session` structs, Argon2id password hashing, session create/validate (`validate_session`, `auth/mod.rs:280-296`, now also rejects a session whose user's `expires_at` has passed — the guest-account expiry mechanism), API token validation both scoped (`validate_api_token`) and unscoped (`validate_api_token_any`), temp-session minting for token requests (`create_temp_session`), role management, and `create_user_ext`/`create_user` (the former adds an optional `expires_at` for time-boxed guest accounts, `auth/mod.rs:139-186`). **Changed (P0-06):** declares `pub mod scope_enforce;` (below) and gained `token_scopes(pool, raw_token) -> Vec<String>` — looks up a raw Bearer token's declared `api_tokens.scopes` independent of the owning user's role, used by `bearer_auth::middleware` to populate the new `TokenScopes` extension (§2). Forbidden zone; this change is under ADR-003's grant. |
| `backend/src/auth/scope_enforce.rs` | **New (P0-06).** The fix for the scope-bypass gap (§4): `pub async fn middleware(...)`, mounted in `api::router()` directly after `bearer_auth::middleware` (§2/§6). No-op for any request without a `TokenScopes` extension (i.e. every session-cookie-authenticated human request passes straight through, unaffected by construction). For token-originated requests, looks up `(method, matched_path)` in the module's own `ROUTE_SCOPES` table (`&[(&str, &str, Requirement)]`, `scope_enforce.rs:55-156`) — `Requirement::NoScopeRequired` for routes that were already fully public before this middleware existed (`/api/health`, `/status`, `/api/capabilities`, `/api/settings/public`, `/v1/models`, `/v1/chat/completions`), `Requirement::Scope(name)` allows through only if the token's scopes contain that exact name, and **a route with no table entry at all is denied by default** — deny-by-default for tokens, not an enumerated denylist. Forbidden zone; under ADR-003's grant. |
| `backend/src/automation/mod.rs` | Stub only — comment-only placeholder for a future YAML automation DSL. **Actual automation job CRUD/execution lives in `backend/src/api/automation.rs`.** |
| `backend/src/backups/mod.rs` | Restic wrapper: `BackupConfig` struct, `is_restic_available()`, run/check/restore-test functions (used by both `api/backups.rs` and the CLI `backup` subcommand in `main.rs`). |
| `backend/src/cluster/mod.rs` | Stub only — just an `is_agent_mode()` helper checking for `--agent` CLI flag; no actual multi-node logic implemented. **Do not confuse with the new fleet node-enrollment feature (`api/node_enroll.rs`, §2/§3)** — that is a distinct, separately-implemented mechanism (phones/tablets/pis over WireGuard) with no relationship to this stub's planned multi-node agent-mode concept. |
| `backend/src/config/mod.rs` | `Config` struct (bind/port/data_dir/frontend_dir/etc.), loaded from file + env, includes the `host_data_dir` vs `data_dir` distinction for containerized VoidTower deployments. |
| `backend/src/containers/mod.rs` | Docker/bollard wrapper: list/action/logs/exec on containers, plus `DeployRegistry` for tracking in-flight `docker compose` deploy PIDs so they can be cancelled gracefully. |
| `backend/src/db/mod.rs` | All schema definition: `init_pool()` runs `run_migrations()` (baseline `CREATE TABLE` block) then a long, ad-hoc sequence of `CREATE TABLE IF NOT EXISTS` / `ALTER TABLE ADD COLUMN` statements accreted over time. No sqlx migration files exist (`backend/migrations/` does not exist — verified via glob). New tables/columns for tiered accounts, fleet node enrollment, and the member self-hosting hub were added here since the prior map (see §3). |
| `backend/src/error.rs` | `AppError` enum (`NotFound`, `Unauthorized`, `Forbidden`, `PolicyDenied`, `BadRequest`, `Conflict`, `FeatureUnavailable`, `TooManyRequests`, `TotpRequired`, `Database`, `Internal`) with a single `IntoResponse` impl mapping each to `(StatusCode, error_code, message)` JSON. |
| `backend/src/monitoring/mod.rs` | `MetricsSnapshot`/`DiskInfo`/`NetworkInfo`/`ProcessInfo`/`GpuInfo` structs and the `MetricsCollector`/`MetricsBroadcaster` used by `main.rs`'s metrics loop and `api/metrics.rs`. |
| `backend/src/networking/mod.rs` | Stub only — comment describing planned interface/route/firewall introspection. **Actual network + firewall logic lives in `api/network.rs` and `api/firewall.rs`.** |
| `backend/src/oidc.rs` | OIDC/Authentik SSO: loads per-instance `OidcSettings` from DB, PKCE flow via the `openidconnect` crate, role mapping from claims. Unchanged since the prior map; still a forbidden-zone file. |
| `backend/src/policy.rs` | The AI/automation policy engine: `PolicyRule` struct, `check()` function, `ApiTokenActor`/`MaybeTokenActor` extractor. **Changed (P0-02), under ADR-001's grant** (which covers this file directly, alongside `voidwatch.rs`/`voidwatch/**` and the three AI-ingress files — distinct from ADR-002, which only covers `db/mod.rs` + `voidwatch/**` for the accompanying schema addition). `check()`'s no-matching-rule fallback is no longer a flat `Allow`: `actor_type == "user"` still defaults to `Allow` (RBAC-governed, not policy-governed), but `api_token`/`automation`/`ai` now consult the new `voidwatch_default_allowlist` table (§3) and `Deny` if no entry matches — default-deny for automated actors, gap-analysis P0.2. Still a forbidden zone otherwise. |
| `backend/src/security/mod.rs` | Stub only — comment describing planned security-scan checks. **Actual session-revocation logic lives in `api/security.rs`.** |
| `backend/src/services/mod.rs` | systemd wrapper: `ServiceInfo`, `ServiceAction` enum (Start/Stop/Restart/Enable/Disable), `is_systemd_available()`, shells out to `systemctl`. |
| `backend/src/storage/mod.rs` | Block device / mount / RAID / SMART introspection structs (`BlockDevice`, `MountInfo`), used by `api/storage.rs`. |
| `backend/src/terminal/mod.rs` | PTY handling via `portable-pty` for the in-browser terminal (`ClientMessage`/`ServerMessage` WS protocol), used by `api/terminal.rs`. |
| `backend/src/vms/mod.rs` | Stub-ish: only `is_libvirt_available()` implemented; comment describes planned `list_vms`/`vm_action`. Local KVM + Proxmox VM logic actually lives in `api/vms.rs` and `api/proxmox.rs`. |
| `backend/src/voidwatch/mod.rs` | **New (P0-01), extended (P0-02), extended again (P0-03).** The single choke point AI/automation-reachable mutating actions must pass through before reaching `policy::check` (EDD §3.2, gap-analysis P0.1). `evaluate(db, actor: Actor, action_kind: ActionKind, action, resource: Resource) -> Verdict` — `Read` actions always `Allow`; `Mutating` actions now first run the P0-03 mode-ladder pre-pass (`mode_pre_pass`, `mod.rs:165-193`) and only fall through to `policy::check` if the pre-pass returns `None` (no mode configured for the resource's scope or globally — the deliberate no-op-safe rollout default, see `mode::get_mode`). Enforced by a source-grep regression test (`mod.rs:533-549`) that fails the build if `api/mcp.rs`, `api/studio.rs`, or `api/integrations.rs` ever call `policy::check(` directly. `ActorKind` still has its reserved fourth variant, `Ai` (`mod.rs:42-60`) — still not constructed by any ingress point as of this map; this is now independent of the scope-bypass gap (§4), which P0-06 closed, and appears to simply be unclaimed follow-up work rather than something blocked on a prerequisite. `Verdict` gained two rungs in P0-03 (`mod.rs:110-116`): `AllowRequireSnapshot(String)` and `RequireApproval(String)`, added as a deliberately breaking exhaustiveness change so every existing caller had to decide what to do with them (see `d378bb1`'s fix in §4 for the bug this caught). |
| `backend/src/voidwatch/allowlist_seed.rs` | **New (P0-02).** `pub(crate) async fn seed_default_allowlist_if_empty(pool)` — one-shot, idempotent backfill of `voidwatch_default_allowlist` (§3) run from `db::init_pool` on every start (no-op once the table has any rows), so the P0-02 default-deny flip doesn't break pre-existing automation usage on upgrade. Sourcing is asymmetric by actor class (documented in detail at `allowlist_seed.rs:17-40`): `api_token` entries are hand-grandfathered from the exhaustive, currently-reachable `ContainerAction`/`ServiceAction` sets in `containers.rs`/`services.rs` (no reliable historical signal exists to mine); `automation` entries are mined from `audit_log` rows with `source = 'odysseus'` (the only actor class with a reliable historical signal); `ai` starts empty (no code path constructs that actor kind yet). Deliberately not in `db/mod.rs` despite touching schema-adjacent concerns — ADR-002's grant explicitly excludes "data migration or backfill logic" from that file, so this lives under `voidwatch/` instead, per the P0-02 task spec. **P0-03 reuses this same table** for Trusted mode's per-resource/per-action allowlist (`mode::trusted_mode_verdict`, below) rather than introducing a second one. |
| `backend/src/voidwatch/mode.rs` | **New (P0-03).** Per-scope Voidwatch mode storage (EDD §3.2). `Mode` enum (`Observer`/`Assisted`/`Trusted`/`Yolo`), `get_mode(db, scope) -> Option<Mode>` — a per-resource scope key (`"{resource_type}:{resource_id}"`, via `scope_for_resource`) falls back to the `"global"` scope row, and `None` (nothing configured anywhere) is the deliberate rollout no-op signal `voidwatch::mod::mode_pre_pass` uses to skip the ladder entirely. `set_mode(db, scope, new_mode, actor) -> Verdict` changes a scope's mode — itself routed through `voidwatch::evaluate` under the reserved action name `"voidwatch.mode.set"` (classified `RiskClass::Irreversible`, ADR-004 item 5: always requires approval, even in YOLO), audit-logged regardless of outcome, and only persists on an `Allow` verdict. **`#[allow(dead_code)]`: no HTTP handler calls `set_mode` yet** — exposing it needs a forbidden-zone grant beyond what P0-03's ADR-001 citation covers; only exercised by this module's own tests today. |
| `backend/src/voidwatch/risk_class.rs` | **New (P0-03).** Two separate, deliberately-not-unified classification tables (module doc comment, `risk_class.rs:1-25`, explains why): `ROUTE_RISK_CLASSES` is an exhaustive `(method, path, RiskClass)` table for every route in `api::router()`, kept honest by a test (`every_registered_route_has_a_risk_class`, `risk_class.rs:677-717`) that parses `api/mod.rs` at test time via a manual balanced-paren scan and fails the build in both directions (new unclassified route, or a stale table entry for a removed route) — `#[allow(dead_code)]` since nothing reads it at runtime yet (most of the routes it covers, e.g. `containers.rs`/`firewall.rs`/`proxmox.rs`, are outside the AI-ingress choke point entirely; it exists as the P0-04 denylist's classification ledger for the whole surface, coordinated but not yet consumed). `for_action(action: &str) -> RiskClass` is the smaller vocabulary `voidwatch::mode_pre_pass` actually consults — the action-name strings the three AI-ingress files pass into `evaluate()` (`"start"`/`"stop"`/`"restart"`/`"automation.run"` → `Mutate`, `"remove"` → `Destructive`, `"voidwatch.mode.set"` → `Irreversible`, and critically **any unrecognized action name also falls through to `Irreversible`** — fail-safe-not-fail-open, and today the only thing standing in for P0-04's not-yet-merged explicit hardcoded denylist for actions this table hasn't seen). `SNAPSHOT_CAPABLE_RESOURCE_TYPES` (`&["vm", "container", "app"]`) backs Trusted mode's mandatory-pre-apply-snapshot clause. |

**Convention note:** several top-level modules (`alerts`, `automation`, `cluster`, `networking`, `security`, and partially `vms`) are comment-only placeholders from an earlier phased plan; the real, shipped logic for those feature areas lives entirely in the corresponding `backend/src/api/*.rs` file. `backend/src/audit/mod.rs` is the one exception that looks like it could be a stub-vs-handler split but isn't — it's genuinely load-bearing shared code. New agents should not assume a top-level module's existence implies (or disproves) working logic — verify against the `api/` file and the module body itself.

---

## 2. API route → handler file map

Source of truth: `backend/src/api/mod.rs:85-439` (`router()` function), the single place all routes are mounted (two sub-routers: `main_router` with global CORS + security headers + `demo_guard::middleware` + `bearer_auth::middleware`, and `embed_router` for app-embed iframes which deliberately skips `security_headers`).

| File | Routes (method — path — handler) |
|---|---|
| `agents.rs` | GET/POST `/api/agents` → `list`/`create`; GET `/api/agents/ws` → `ws_handler`; GET `/api/agents/export` → `export`; POST `/api/agents/import` → `import`; PUT/DELETE `/api/agents/:id` → `update`/`delete`; GET/POST `/api/agents/:id/status` → `get_status`/`post_status` |
| `ai.rs` | GET `/api/ai/llama` → `llama_status`; POST `/api/ai/llama/unload` → `llama_unload` |
| `ai_ask.rs` | POST `/api/ai/ask` → `ask` |
| `ai_context.rs` | GET `/api/ai/context` → `get_context`. **Changed (P0.5)**: the serialized context bundle now passes through `super::mcp::redact::redact_for_ai` (`ai_context.rs:159`) before being returned — see §4. |
| `ai_providers.rs` | GET/POST `/api/ai/providers` → `list`/`create`; PUT/DELETE `/api/ai/providers/:id` → `update`/`delete`; GET `/api/ai/providers/:id/health` → `health` |
| `alerts.rs` | GET `/api/alerts` → `list`; POST `/api/alerts/:id/acknowledge` → `acknowledge`; POST `/api/alerts/:id/resolve` → `resolve`; DELETE `/api/alerts/:id` → `delete_alert` |
| `apps.rs` | GET `/api/apps/catalog`, `/detect-env`, `/deployed`; POST `/deploy`, `/deploy/cancel/:project_name`, `/deploy-custom`, `/open-ui`; DELETE `/api/apps/:project_name`; POST `start`/`stop`/`restart`/`redeploy`/`convert`/`pull`/`env`/`expose`/`delete-volumes`/`purge`; GET `logs`/`status`; GET/POST `compose`; GET `/detect-external`; POST `/adopt`; plus GET `/api/apps/embed/:project_name/*path` → `embed_proxy` in the **embed_router**. **Changed**: `deploy`/`deploy-custom` now branch on `user.role == "member"` to resolve app access, storage root, target node, and quota via `members::*` helpers (§1) before deploying — see `apps.rs` diff, no route paths changed, only handler bodies. |
| `audit.rs` | GET `/api/audit` → `list`, delegating to `crate::audit::list` (§1). |
| `auth.rs` | GET `/api/health` → `health`; POST `/api/auth/login`,`/logout`; GET `/api/auth/me`; POST `/api/auth/bootstrap`; GET `/api/auth/oidc/login`,`/callback`,`/status`; GET/PUT `/api/oidc/config` |
| `automation.rs` | GET/POST `/api/automation` → `list`/`create`; DELETE/PATCH `/api/automation/:id` → `delete`/`update`; POST `/api/automation/:id/run` → `run_now`; GET `/api/automation/:id/runs` → `runs` |
| `backups.rs` | GET/POST `/api/backups` → `list`/`create`; POST `/api/backups/:id/delete-plan`,`/run`,`/check`,`/restore-test`; DELETE `/api/backups/:id` |
| `bearer_auth.rs` | Not a route module — global middleware (`middleware()`), applied via `axum::middleware::from_fn_with_state` (`mod.rs:459`, outermost layer — see §6 for layer-ordering note). **Changed (P0-06):** now also resolves and caches the token's declared scopes (`auth::token_scopes`) alongside the session it mints, and inserts a new `pub struct TokenScopes(pub Vec<String>)` extension onto the request (`bearer_auth.rs:10-18`) so the new `scope_enforce::middleware` (§1) has something to check downstream; the `token_sessions` cache (`AppState`, `main.rs`) is now `token_hash -> (session_id, expires_at, scopes)` instead of just `(session_id, expires_at)`. Forbidden zone; under ADR-003's grant. |
| `capabilities.rs` | GET `/api/capabilities` → `get_capabilities` |
| `containers.rs` | GET `/api/containers`, `/images`; POST `/:id/action`; GET `/:id/logs`, `/logs/stream`, `/exec`, `/compose`; POST `/compose/propose`, `/compose/apply` |
| `demo_guard.rs` | Not a route module — global middleware (new), `mod.rs:432`. Blocks any non-GET `/api/*` request from a `role = "demo"` session with a `403 {"error":{"code":"demo_mode",...}}` body, except `/api/auth/logout`. Layered between `security_headers` and `bearer_auth` in source order (see §6). |
| `diagnostics.rs` | GET `/api/diagnostics` → `get_diagnostics` |
| `disaster.rs` | POST `/api/disaster/export-config`, `/import-config`, `/emergency-reset-admin`, `/emergency-disable` |
| `events.rs` | GET `/api/events/stream` → `stream_handler` |
| `files.rs` | GET `/api/files/roots`,`/list`,`/read`,`/activity`,`/raw`; POST `/write`,`/mkdir`,`/rename`; DELETE `/delete` |
| `firewall.rs` | GET `/api/firewall` → `get_status`; POST `/rules` → `add_rule`; POST `/rules/delete` → `delete_rule`; POST `/action` → `firewall_action` |
| `integrations.rs` | GET `/api/integrations/scopes`; GET/POST `/tokens`; DELETE `/tokens/:id`; GET/POST `/odysseus/config`; GET `/odysseus/manifest`, `/odysseus/theme`; GET `/events` → `event_stream`; POST `/webhooks` → `webhook`; GET `/actions` → `recent_actions`. **Changed**: both the `automation_id`-triggered path (`run_automation_job()`, `integrations.rs:737-`) and `webhook()`'s structured resource-action path (`integrations.rs:859-`) call `voidwatch::evaluate` instead of `policy::check` directly. **Changed again (P0-03)**: both call sites now route their verdict through a shared `verdict_block_reason()` helper (`integrations.rs:726-735`) that blocks on anything but a plain `Allow` — added by `d378bb1` after adversarial review caught `webhook()`'s structured-action branch silently treating `AllowRequireSnapshot` as `Allow` for `container.restart/start/stop`, skipping Trusted mode's mandatory pre-apply snapshot (see §4). **Changed again (P0-06):** `POST /tokens` (`create_token`) now accepts an optional `tier` field alongside the pre-existing explicit `scopes` array; when set, it looks up `pub const CAPABILITY_TIERS: &[(&str, &[&str])]` (`integrations.rs:68-93`: `"read"`, `"deploy"`, `"exec"`, `"admin-never"` — the last mints an empty scope set) and the tier's fixed scope subset **replaces** `scopes` server-side rather than being merged with it, so a caller cannot widen a tier by also passing `scopes`. Forbidden zone; under ADR-003's grant for the scope/token-related changes only (this file is also covered by ADR-001 for the pre-existing, unrelated `voidwatch::evaluate` routing above — don't conflate the two ADRs in future commit messages, per the P0-06 task spec's own note). |
| `lxc.rs` | GET `/api/lxc` → `list`; GET `/:vmid/config`; POST `/:vmid/action` |
| `mcp.rs` | GET `/api/mcp` → `sse_handler`; POST `/api/mcp/message` → `message_handler`. **Changed**: `tools/call` dispatch now goes through `invoke_tool()` gated by `voidwatch::evaluate` — see §4. **Changed again (P0.5)**: `invoke_tool()`'s successful-result path also runs the serialized tool output through `redact::redact_for_ai` (`mcp.rs:573`) before returning it. Declares two new submodules via `#[path = ...]` (`mcp.rs:26-30`) rather than top-level `api::` modules: `pub mod redact` (backed by `api/redact.rs`, see §4) and `pub(crate) mod test_support` (backed by `api/test_support.rs`) — cite them as `api::mcp::redact`/`api::mcp::test_support` in new work, not `api::redact`. |
| `members.rs` | **New.** Self-hosting hub, admin + self-service. GET `/api/members` → `list_members`; GET `/api/members/me/access` → `get_my_access`; GET `/api/members/me/nodes` → `list_my_nodes`; GET `/api/members/:user_id/access` → `get_access`, POST same path → `grant_access`; DELETE `/api/members/:user_id/access/:app_id` → `revoke_access`; POST `/api/members/:user_id/custom-deploy` → `set_custom_deploy`; POST `/api/members/:user_id/storage` → `set_quota`; POST `/api/members/:user_id/drives` → `add_drive`; DELETE `/api/members/drives/:drive_id` → `remove_drive`. Also exposes `pub(crate)` deploy-path resolution helpers consumed directly by `apps.rs` (not routes): `check_member_app_access`, `member_can_deploy_custom`, `check_member_quota`, `resolve_member_storage_root`, `resolve_member_target_node`, `allocate_member_port`, and the periodic `poll_member_storage` (called from `main.rs`, not a route). |
| `metrics.rs` | GET `/api/metrics/current`, `/api/metrics/ws` |
| `models.rs` | GET/POST `/api/models*` (download, active, load, llama-config, ollama-config, ollama tags/pull/create); DELETE `/api/models/:filename`; **no-auth** GET `/v1/models`, POST `/v1/chat/completions` (explicitly commented "OpenAI-compatible proxy — no auth, Odysseus-facing") |
| `mod.rs` | Router assembly only — not a handler file. |
| `mods.rs` | GET `/api/mods` → `get_status`; POST `/config`,`/fetch`; GET `/diff`; POST `/apply`,`/rollback` |
| `nav_config.rs` | GET/POST/DELETE `/api/nav-config`, `/api/nav-config/default` |
| `network.rs` | GET `/api/network/neighbors` → `neighbors` |
| `node_enroll.rs` | **New.** Fleet node enrollment (phones/tablets/pis joining over WireGuard). POST `/api/nodes/pairing-code` → `create_pairing_code` (admin, mints a single-use 15-minute code); POST `/api/nodes/enroll` → `enroll` (no session — pairing-code-authenticated, atomically claims the code, provisions a WireGuard peer via `wireguard::add_peer_core`, mints a node-scoped heartbeat token, **not** an `api_tokens` row — see the doc comment at `node_enroll.rs:64-68` explaining why: a real API token would grant a full admin-equivalent session via `bearer_auth`'s generic token→session resolution); GET `/api/nodes` → `list` (admin); DELETE `/api/nodes/:id` → `delete_node` (admin, also removes the WireGuard peer via `wireguard::delete_peer_core`); POST `/api/nodes/:id/heartbeat` → `heartbeat` (node-token-authenticated, no session — updates `last_seen`/`last_telemetry`). **Correction to the prior map:** the admin-facing routes are not entirely unwired — `mobile/src/api/client.ts`'s `nodes` namespace (`client.ts:102-113`) defines typed wrappers for all five routes (`list`, `mintPairingCode`, `enroll`, `remove`, `heartbeat`), not just `enroll`. What's actually true, re-verified by grepping for `api.nodes.` usage (not just client-method definitions) across both `mobile/src/` and `frontend/src/`: only `api.nodes.enroll` is called from any screen component (`mobile/src/screens/NodeEnrollmentScreen.tsx:23`) — `list`/`mintPairingCode`/`remove`/`heartbeat` are defined but dead, called from no screen. `frontend/src/api/client.ts` has no `nodes` namespace at all. The prior map's "no frontend consumer exists yet ... neither wires them" was imprecise — the mobile client *declares* the full surface, it's just unused past `enroll`. |
| `plugins.rs` | GET/POST `/api/plugins`; PATCH/DELETE `/api/plugins/:id`; GET `/plugin-assets/:id/*path` → `serve_asset` (embed_router) |
| `policy.rs` | GET/POST `/api/policy/rules`; PATCH/DELETE `/api/policy/rules/:id`; POST `/api/policy/check` → `check_policy` |
| `proxmox.rs` | Large surface under `/api/proxmox/...`: hosts CRUD, nodes/vms/storage/tasks/backup-jobs listing, VM lifecycle (start/stop/shutdown/reboot/reset/suspend/resume), snapshot CRUD + rollback, VNC proxy, disk passthrough, LXC deploy, node disk list/SMART/wipe/init, storage content upload/delete |
| `proxy.rs` | GET/POST `/api/proxy`; GET `/nginx-setup`; POST `/nginx/action`; GET `/nginx/logs`,`/nginx/status`; POST `/ai-auto`; DELETE/PUT `/:id`; POST `/:id/toggle`; GET `/:id/health` |
| `secrets.rs` | GET/POST `/api/secrets`; DELETE/PATCH `/:id`; GET `/:id/reveal`; POST `/:id/rotate` |
| `security.rs` | GET `/api/security/sessions`; POST `/revoke-others`; DELETE `/:id` |
| `services.rs` | GET `/api/services`; GET `/:name`; POST `/:name/action`; GET `/:name/logs` |
| `settings.rs` | GET `/api/settings/public`; GET/POST `/ai-url`,`/general`,`/notifications`; POST `/notifications/test`; **new** GET/POST `/api/settings/mfa-policy` → `get_mfa_policy`/`set_mfa_policy` (admin — reads/writes which roles require mandatory TOTP enrollment, default `["owner","admin"]`, `settings.rs:409-475`). |
| `status.rs` | GET `/status` (public, unauthenticated); GET/POST `/api/status-checks`; DELETE `/api/status-checks/:id` |
| `storage.rs` | GET `/api/storage/devices`,`/mounts`,`/fstab`,`/smart/:dev`,`/raid`,`/paths`; POST `/mount`,`/umount`,`/fstab`,`/raid/create`,`/raid/stop`,`/format`; DELETE `/fstab/:idx`; POST `/paths` |
| `studio.rs` | GET `/api/studio/status`; POST `/image/generate`; GET `/images/:filename`; POST `/tts/generate`; GET `/audio/:filename`; POST `/stt/transcribe`; GET `/gallery`; DELETE `/gallery/:kind/:filename`; GET `/mcp/tools` → `mcp_tools`; POST `/mcp/invoke` → `mcp_invoke`. **Changed**: `mcp_invoke` now passes `Actor { kind: ActorKind::User }` into `mcp::invoke_tool` (`studio.rs:697-706`), which is both voidwatch-gated and redaction-gated (§4) — `mcp_invoke` itself makes no separate redaction call; it inherits both by construction from reusing `invoke_tool`, proven by a same-file test (`studio.rs:752`, `redaction_corpus_never_appears_in_studio_mcp_invoke_output`). |
| `system.rs` | GET `/api/system/version`,`/update-check`; POST `/restart`,`/update` |
| `tabs.rs` | GET/POST `/api/tabs`; PUT `/order`; GET `/export`; POST `/import`; PUT/DELETE `/:id` |
| `tags.rs` | GET/POST `/api/tags`; GET `/for`,`/map`; POST `/assign`,`/unassign`; DELETE/PATCH `/:id` |
| `terminal.rs` | GET `/api/terminal/ws`; GET/POST `/ssh/sessions`; DELETE/PUT `/ssh/sessions/:id`; GET `/ssh/ws`; GET/POST `/local/sessions`; PUT/DELETE `/local/sessions/:id` |
| `timeline.rs` | GET `/api/timeline` → `list` |
| `totp.rs` | POST `/api/auth/totp/setup`,`/enable`,`/disable` |
| `updates.rs` | GET/POST `/api/updates/voidtower*`, `/odysseus*`, `/docker*`, `/os*` (info/check/apply/rollback per target) |
| `users.rs` | GET/POST `/api/users`; POST `/me/password`; DELETE `/:id`. **Changed**: `create` now accepts `guest`/`demo`/`member` roles in addition to `admin`/`operator`/`viewer` (`users.rs:86`), and requires (and validates a future) `expires_at` when `role == "guest"`. |
| `vms.rs` | GET `/api/vms/local`; POST `/local/action`; GET/POST `/proxmox/config`; GET `/proxmox/vms`; POST `/proxmox/action`,`/proxmox/test` |
| `webhooks.rs` | GET/POST `/api/webhooks`; PATCH/DELETE `/:id`; POST `/:id/test` — **outbound** notification webhooks (ntfy/discord/slack/generic), distinct from `integrations.rs`'s inbound Odysseus webhook |
| `wireguard.rs` | GET `/api/wireguard`; POST `/peers`; DELETE `/peers/:id`. **Changed**: the peer-add/peer-remove logic behind the HTTP handlers was factored into `pub(crate) add_peer_core`/`delete_peer_core` (`wireguard.rs:404-467`, `513-...`) so `node_enroll.rs`'s pairing-code-authenticated enrollment/deletion flow can reuse the exact same WireGuard provisioning code as the admin-session HTTP routes, rather than duplicating it. |

---

## 3. DB table → creation site map

All schema lives in `backend/src/db/mod.rs`. No `backend/migrations/` directory exists (verified: glob returns no files).

**Baseline tables — `run_migrations()`:**

| Table | Stores |
|---|---|
| `users` | Local accounts: username, argon2 hash, role, force-password-change flag. **Column added** (`init_pool`, not the baseline block): `expires_at` — guest-account expiry (see below). |
| `sessions` | Browser session cookies (`vt_session`), expiry, IP/UA |
| `api_tokens` | Scoped bearer tokens: `token_hash`, `scopes` (JSON array), expiry |
| `audit_log` | Append-only action log: actor_type, action, resource, outcome, details |
| `settings` | Generic key/value config store (Odysseus config, AI URL, MFA policy, etc.) |
| `themes` | UI theme definitions |
| `alerts` | Active/acknowledged/resolved alert records |
| `status_checks` | User-defined uptime/health checks |
| `backup_configs` | Restic backup job definitions + last run/check/restore-test status |
| `deployed_apps` | App Vault deployment records. **Columns added** (`init_pool`): `owner_user_id`, `storage_root`, `target_node_id` — nullable member-tenancy columns; NULL on every row means "admin-deployed on the primary host," identical to pre-feature behavior. |
| `proxy_configs` | Reverse-proxy domain/upstream rules |
| `node_registry` | Placeholder for multi-node agents (unused — no `--agent` implementation). **Do not confuse with the new `nodes` table below** — unrelated features. |
| `wireguard_peers` | WireGuard peer public keys/allocated IPs |
| `secrets` | AES-256-GCM encrypted secret values |
| `automation_jobs` | Shell-command automation job definitions + schedule |
| `automation_runs` | Execution history for automation jobs |
| `tags` / `resource_tags` | Resource tagging (used by `policy.rs`'s `resource_tag` matching) |
| `ssh_sessions` / `local_sessions` | Saved terminal session profiles |
| `proxmox_hosts` | Registered Proxmox VE endpoints |
| `agent_registry` / `agent_status` | Dev-team agent visualization registry (unrelated to the EDD's device agent) |
| `custom_tabs` | User-defined custom UI tabs |
| `user_nav_config` | Per-user nav layout customization |

**Post-baseline accretion — `init_pool()` (each a separate `CREATE TABLE IF NOT EXISTS` or `ALTER TABLE ADD COLUMN`, executed unconditionally on every start):**

| Table/column | Stores |
|---|---|
| `webhook_configs` | Outbound notification webhook configs (ntfy/discord/slack/generic) |
| various early `ALTER TABLE` additions | force_password_change, allow_embed, backup check/restore-test columns, totp columns, audit source, secrets version, api_tokens.secret_ids, embed_port, ssh password_enc, deployed_apps.origin |
| `policy_rules` | Policy engine rules (actor_type/action/resource_type/resource_tag/effect/priority) — seeded with 3 default deny rules on first install |
| `plugins` | Installed plugin registry (name/version/entry/nav_group) |
| `oidc_config` | Authentik/OIDC SSO settings |
| more `proxy_configs` `ALTER TABLE` additions | custom_headers, rate_limit_rpm, basic_auth, websocket_extended, cache_static, health_status/checked_at/latency_ms |
| `ai_providers` | Multi-provider AI config (kind/base_url/api_key_ref/model/priority) |
| `users.expires_at` (`ALTER TABLE`, `db/mod.rs:284`) | **New.** Tiered accounts: guest accounts (role=`guest`) expire automatically at this timestamp; demo accounts (role=`demo`) reuse the existing role-string ladder rather than a separate flag column and are instead sandboxed read-only by `demo_guard::middleware` (§2). |
| `node_pairing_codes` (`db/mod.rs:309`) | **New.** Single-use, short-lived (15 min) codes an admin mints (`node_enroll::create_pairing_code`) that a device redeems once to enroll; `token_hash` + `used_at`/`expires_at` for atomic single-claim semantics. |
| `nodes` (`db/mod.rs:322`) | **New.** Enrolled fleet devices (phones/tablets/pis): `display_name`, `device_type`, `owner_user_id`, `wg_peer_id`/`wg_public_key` (its WireGuard identity), `token_hash` (heartbeat-only bearer token, **not** an `api_tokens` row), `last_seen`/`last_telemetry`, `agent_capable`, `approved`. |
| `member_app_access` (`db/mod.rs:345`) | **New.** Per-member catalog app allow-list; `(user_id, app_id)` composite PK — a member sees only apps an admin explicitly granted here. |
| `member_storage` (`db/mod.rs:359`) | **New.** One row per member: soft storage quota (`quota_bytes`, default 5 GiB) + container-count cap (`max_apps`, default 5) + last-polled `used_bytes`/`last_check_at`. |
| `member_drives` (`db/mod.rs:374`) | **New.** Admin-registered, already-mounted host paths assigned to a member as extra deploy-time storage capacity. VoidTower never formats/partitions/mounts anything here — purely bookkeeping over pre-existing mounts. |
| `member_settings` (`db/mod.rs:392`) | **New.** Currently just the member's `can_deploy_custom` opt-in flag; kept separate from `member_storage`/`member_app_access` since it's neither quota nor catalog-grant data. |
| `voidwatch_default_allowlist` (`db/mod.rs:422`, created here under ADR-002's grant but backfilled by `voidwatch::allowlist_seed::seed_default_allowlist_if_empty`, §1) | **New (P0-02).** `(actor_type, action, resource_type)` tuples that `policy::check` treats as `Allow` when no `policy_rules` row matches and the actor class is not `user` — the default-deny escape hatch for `api_token`/`automation`/`ai` actors. **Reused, not extended, by P0-03** for Trusted mode's per-resource/per-action allowlist (§4) — same table, second consumer. Guarded against schema drift by a golden-schema test, `backend/tests/schema_golden.sql` (test fn at `db/mod.rs:752`, see §6). |
| `voidwatch_mode_settings` (`db/mod.rs:442`, created here under ADR-002's grant) | **New (P0-03).** `(scope, mode, updated_at, updated_by)`, `scope` PK — either the literal string `"global"` (instance-wide default) or `"{resource_type}:{resource_id}"` (per-device/app override, derived from the `Resource` `voidwatch::evaluate()` already receives, so no ingress call site passes scope explicitly). Additive only, no seed row — an absent row means "mode ladder not configured for this scope," which `voidwatch::mode::get_mode` treats as a deliberate no-op (mode ladder skipped, falls through to pre-existing P0-01/P0-02 behavior) rather than something to backfill. Also covered by the `schema_golden.sql` drift test above. |

**Role ladder as of this map:** `owner`, `admin`, `operator`, `viewer`, `guest`, `demo`, `member` — see `api/users.rs:86` (create-time validation) and `api/settings.rs`'s `valid_role()` (MFA-policy validation) for the two places this set is enumerated; they must be kept in sync manually (no shared const — worth flagging if a task touches either).

---

## 4. AI-reachable ingress points (exhaustive, verified in source)

This is the most safety-critical section. Every claim below cites the exact call site, re-verified directly against source for this regeneration. **The scope-bypass gap (bearer-token scoping) is now CLOSED in `main`** — P0-06 merged this cycle (see the preamble and the rewritten "scope-bypass gap" subsection below). P0.4's hardcoded irreversibility denylist is still written and pushed but **not merged**, blocked on the same unresolved item-10 ambiguity as the prior three maps.

### The voidwatch choke point (P0-01 → P0-02 → P0-03) — what each phase actually changed

`voidwatch::evaluate(db, actor, action_kind, action, resource) -> Verdict` (`voidwatch/mod.rs:132-159`) is the sole caller of `policy::check` from the three AI/automation-reachable ingress files. Enforced by a source-grep regression test (`voidwatch/mod.rs:533-549`) that fails the build if `api/mcp.rs`, `api/studio.rs`, or `api/integrations.rs` ever call `policy::check(` directly. Verified as true today for all three files.

**P0-01** was a no-op-by-default choke point: `Read`-classified actions always `Allow`; `Mutating` actions got exactly the same `policy::check` verdict as before. Its only effect was structural — `mcp.rs`/`studio.rs` started consulting `policy_rules` at all.

**P0-02 ended the no-op.** `policy.rs`'s `check()` now default-**denies** when no `policy_rules` row matches, for `actor_type` in `api_token` / `automation` / `ai` (`user` stays RBAC-governed, default-allow). Escape hatch: `voidwatch_default_allowlist` (§3), backfilled once on upgrade by `voidwatch::allowlist_seed::seed_default_allowlist_if_empty` (§1).

**P0-03 adds a pre-pass in front of both of the above** (`mode_pre_pass`, `voidwatch/mod.rs:165-193`), evaluated first, for *every* actor class equally (ADR-004 constraint 2 — the ladder isn't AI-only):
- Looks up the current `mode::Mode` for the resource's scope (falling back to the `"global"` scope row, §3). **If nothing has ever been configured — the default state of every install today, since no HTTP handler can set a mode yet (see §1's `mode.rs` entry) — the pre-pass is a complete no-op and falls straight through to P0-01/P0-02 behavior exactly as before.** This is why every ingress point below still behaves identically to the prior map's description in practice, despite the new code being live.
- `Observer` → `Deny` unconditionally. `Assisted` → `RequireApproval` unconditionally. `Yolo` → `Allow` unless the action's `risk_class::for_action()` is `Irreversible`, in which case `RequireApproval` (the "irreversibility denylist exception," coordinated with — but not dependent on — P0-04's not-yet-merged explicit list; see below). `Trusted` → `RequireApproval` for `Destructive`/`Irreversible`-risk actions always; for `Mutate`-risk actions, `Allow` only if `(actor_type, action, resource_type)` is on `voidwatch_default_allowlist` (same table P0-02 introduced, reused rather than duplicated) — allowlisted actions against a `SNAPSHOT_CAPABLE_RESOURCE_TYPES` resource (`vm`/`container`/`app`) get `AllowRequireSnapshot` instead of a plain `Allow`.
- `Verdict::AllowRequireSnapshot(String)` and `Verdict::RequireApproval(String)` are new terminal states (§1). **Neither ingress point below has anywhere to act on them today** — no approvals queue exists, and neither `mcp.rs`/`studio.rs`'s tool dispatch nor `integrations.rs`'s webhook path has a snapshot mechanism wired in — so both call sites treat every non-`Allow` verdict as a block (fail closed), documented in detail at `integrations.rs:726-735`'s `verdict_block_reason()` and applied identically by `mcp.rs`'s caller. **This was a live bug for one release**: `d378bb1` (same-day follow-up to `231ba2d`) fixed `integrations.rs`'s `webhook()` structured-action branch, which had silently treated `AllowRequireSnapshot` as `Allow` — reachable there (unlike `run_automation_job`'s fixed `"automation_job"` resource type) because `container` is snapshot-capable, so a Trusted-mode-mandated pre-apply snapshot was being skipped for `container.restart/start/stop`. Fixed with a shared helper specifically so the two call sites' handling can't drift apart again.

**Practically, as of this map:** unless an operator has explicitly configured a mode (no UI/API path to do so exists yet), every ingress point below behaves exactly as the P0-02-era map described. The mode ladder is live code, correctly tested (`voidwatch/mod.rs`'s `exhaustive_mode_by_risk_class_matrix` test enumerates all 16 mode×risk×allowlisted combinations), but dormant until wired to an operator-facing control.

### Risk classification (P0-03, `voidwatch/risk_class.rs`)

Two independent tables (§1) — `for_action()` is the one the mode ladder above actually consults; `ROUTE_RISK_CLASSES` is a separate, exhaustively-tested-but-not-yet-consumed per-route ledger intended as the classification P0-04's denylist will hang off. **`for_action()`'s fail-safe default is `Irreversible` for any action name it hasn't explicitly classified** — today this is the *only* enforcement of irreversibility-style caution in `main` (P0-04's purpose-built hardcoded list, `voidwatch/denylist.rs`, is not merged — see below), and only takes effect once a mode is configured (per the dormancy note above).

### Secrets redaction on the AI context path (P0.5, `api/redact.rs`, declared as `api::mcp::redact`)

New defense-in-depth layer, independent of and downstream from the voidwatch gating above — it runs on the *response*, not the request, and applies regardless of verdict (i.e. it also redacts an already-authorized `Allow` response). Two layers (module doc comment, `redact.rs:6-16`): (1) `redact_known_values` — exact-match redaction of every currently-stored `secrets` table value, decrypted via the same `api::secrets::decrypt` routine the Secrets panel uses; (2) `redact_patterns` — heuristic redaction of secret-*shaped* substrings not registered as VoidTower secrets at all (keyword=value pairs for a fixed keyword list including `password`/`api_key`/`token`/`credential`/etc., PEM private-key blocks, `Bearer <token>` headers, AWS `AKIA...` access keys), deliberately narrow (requires a keyword+separator or a structural marker) so hashes/UUIDs survive untouched (`redaction_does_not_break_non_secret_content` test). `redact_for_ai(state, text)` is the convenience entry point that fetches current known values and runs both layers. **Wired into exactly three places**, matching the module doc comment's claimed scope:
- `api/mcp.rs`'s `invoke_tool()` (`mcp.rs:573`) — every MCP tool-call result, before it's returned.
- `api/studio.rs`'s `mcp_invoke` — not a separate call; inherited automatically by reusing `mcp::invoke_tool` (§2), proven by a dedicated same-file test.
- `api/ai_context.rs`'s `get_context` (`ai_context.rs:159`) — the serialized context bundle.

Not wired into `api/ai_ask.rs` (`POST /api/ai/ask`) directly — that handler's own response isn't a tool-output/context-bundle passthrough in the same sense; verify against source before assuming redaction coverage there if a future task touches it.

### Ingress points and their current gating status

**`backend/src/api/mcp.rs` — MCP server (`GET /api/mcp`, `POST /api/mcp/message`)**
- Auth: `check_mcp_auth()` requires `odysseus.mcp_enabled` setting = `"true"` and a valid Bearer token via `auth::validate_api_token_any` (unscoped — any valid token from any user works).
- `tools/call` dispatch routes through `invoke_tool()` (`mcp.rs:522-`), which classifies each tool via `tool_action_kind()`/`READ_ONLY_TOOLS` (`mcp.rs:498-512`: `list_nodes`, `get_node_metrics`, `list_containers`, `list_services`, `list_alerts`, `get_container_logs`, `list_routes`, `read_file`, `search_code`, `get_template` — still the only 10 tools registered, still all read-only) and calls `voidwatch::evaluate(db, Actor{ApiToken}, action_kind, name, Resource{"mcp_tool", name})` before dispatch, then redacts the successful result (`redact::redact_for_ai`, above) before returning it. Any tool not in `READ_ONLY_TOOLS` is treated as `Mutating` by default — deny-capable by default, not silently ungated.
- `tool_read_file`/`tool_search_code` (delegating to `ai_context.rs`) still do path-traversal sanitization but otherwise expose the entire repo tree read-only to any valid token holder — unchanged, and still `Read`-classified (correctly, since it's read-only). **Not covered by P0.5's redaction** (it's a `Read`-classified tool result, but the redaction wiring above is applied to `invoke_tool`'s output uniformly regardless of classification — worth re-verifying directly if this matters for a future task, since read-only-but-secret-containing file content is exactly the scenario P0.5 exists for).

**`backend/src/api/studio.rs` `mcp_invoke` (`POST /api/studio/mcp/invoke`)**
- Auth: `require_user()` (session cookie only) — the in-UI "Studio" panel's tool-test button.
- Calls `super::mcp::invoke_tool(&state, Actor{ kind: ActorKind::User }, &req.name, req.arguments)` (`studio.rs:703-706`) — the same gated, now also redaction-covered, entry point `mcp.rs` uses. `ActorKind::User` intentionally matches only `"*"`-scoped policy rules, preserving the existing convention that session-authenticated humans are governed by RBAC (`auth::User::role`), not `policy_rules`.

**`backend/src/api/integrations.rs` `manifest` (`GET /api/integrations/odysseus/manifest`)**
- Unchanged: no auth check at all, purely a JSON tool-surface description consumed by Odysseus, does not itself execute anything. The real ingress points it documents are analyzed below and under "the scope-bypass gap."

**`backend/src/api/integrations.rs` `webhook` (`POST /api/integrations/webhooks`)** — gated:
- Auth: constant-time comparison against `odysseus.webhook_secret`, plus `odysseus.enabled`/`emergency_disabled` checks.
- Structured resource actions (`container.restart/start/stop`, `service.restart/start/stop`) call `voidwatch::evaluate` (`integrations.rs:933-`, actor kind `Automation`), verdict handled via `verdict_block_reason()` (above — blocks on anything but `Allow`).
- `run_automation_job()` (`integrations.rs:737-`, the `automation_id`-triggered path) calls `voidwatch::evaluate(db, Actor{Automation}, Mutating, "automation.run", Resource{"automation_job", automation_id})` before running `job.command` via `bash -c`, same block-on-non-`Allow` handling. It is still true that any authenticated non-viewer UI user can create arbitrary-command automation jobs via `POST /api/automation` — unaffected and out of scope for voidwatch (human, session-authenticated action).

**`backend/src/api/ai_ask.rs` (`POST /api/ai/ask`)** — unchanged: session-cookie-only, human-initiated chat assistant; documents an external MCP tool server (`odysseus-mcp-servers/voidtower_server.py`) whose write tools call the same underlying REST endpoints, so the real ingress control point for those is the REST handler's own auth (see the scope-bypass gap below), not this file. Not covered by P0.5's redaction wiring (see above).

**`backend/src/api/webhooks.rs` (`/api/webhooks*`)** — unchanged: outbound notification-webhook system, admin-gated, not an AI ingress point; do not confuse with `integrations.rs`'s inbound `webhook` handler.

**`backend/src/api/ai_context.rs`** — `get_context` now redacts its output (§2, above) — the one ingress point whose behavior actually changed independent of voidwatch this cycle.

**`backend/src/api/ai.rs`, `ai_providers.rs`, `agents.rs`** — unchanged from the prior map's analysis: admin/session-gated configuration or read-only surfaces, not newly-relevant to voidwatch or redaction.

### The irreversibility denylist (P0-04) — written, pushed, **not merged**

`docs/adr/ADR-004-irreversibility-denylist-reconciliation.md` is Accepted (signed 2026-07-09), and `devteam/P0-04-irreversibility-denylist` reportedly implements a hardcoded, non-API-editable denylist per EDD §10.3 (self-update trigger, disaster-recovery import/reset, secrets export, policy/mode edits, disk wipe/format, firewall disable) on top of `risk_class`. **As of this map's HEAD, `backend/src/voidwatch/denylist.rs` still does not exist in `main`** — re-verified directly (`find backend/src/voidwatch` lists only `mod.rs`, `allowlist_seed.rs`, `mode.rs`, `risk_class.rs`). Until it merges, `risk_class::for_action()`'s fail-safe-default-to-`Irreversible` behavior (above) is the only thing standing in for it, and only once a mode is configured. One item (#10 of 12, "last remaining snapshot/backup of a resource") is escalated as a genuine textual ambiguity in ADR-004 itself — whether it means the last `backup_configs` schedule row (`api/backups.rs`, config coverage, not data loss) or the last Proxmox VM snapshot (`api/proxmox.rs`, actual data loss) — and is still parked, unanswered, in `.devteam/blocked/P0-04-irreversibility-denylist.md` as of this map. A task consuming this map for P0.4 work should check `.devteam/blocked/`/`.devteam/queue/` directly before assuming either the file's existence or its exact denylist contents.

### The scope-bypass gap — CLOSED in `main` (P0-06, merged `7fa1b77`)

Three prior maps tracked this as open; **it is fixed as of this regeneration**, verified directly against `backend/src/auth/scope_enforce.rs`, `backend/src/api/bearer_auth.rs`, and `backend/src/api/mod.rs`'s layer ordering (§1/§2/§6). The original bug, for reference:

1. `bearer_auth::middleware` is mounted globally. A request with `Authorization: Bearer <token>` and no session cookie calls `auth::validate_api_token_any()` — checked only that the token hash exists and wasn't expired, **never scopes** — and minted a full 1-hour session via `auth::create_temp_session()` for the token's owning user.
2. Every downstream handler's `require_user`/`require_admin`/`require_owner` helper then saw an ordinary session and applied the token owner's actual role, not the token's declared scope.
3. `ALL_SCOPES` (`integrations.rs:29-55`) was consulted in exactly two places in the whole codebase (`events.rs`, `integrations.rs`'s own `event_stream`). Every other scope-implying endpoint performed no scope check at all.
4. Net effect: a token minted with only `metrics:read`, if owned by an admin/owner user, could call any admin/owner-gated endpoint.

**The fix (P0-06, ADR-003):** `bearer_auth::middleware` now also resolves the token's declared scopes and attaches them via a new `TokenScopes` extension (§1/§2). A new, single mandatory middleware, `auth::scope_enforce::middleware`, is mounted directly after it (§6's layer-ordering note) and is the sole choke point ADR-003 specified — not a per-handler patch across the ~20 files that each define their own `require_user`/`require_admin`/`require_owner`. It is a structural no-op for any request without `TokenScopes` (proven end-to-end by `human_session_cookie_login_is_unaffected_by_scope_changes` in `api/scope_bypass_tests.rs`, exercised via a real `POST /api/auth/login`), so session-cookie humans are provably unaffected. For token-originated requests, its `ROUTE_SCOPES` table (`scope_enforce.rs:55-156`) is **deny-by-default**: a route with no entry is closed to tokens regardless of the owning user's role — the "deliberate, tested choice" ADR-003 required, verified by `unlisted_route_scope_behavior_is_deliberate_and_tested` (added in the follow-up commit `14b0da3`, using `GET /api/webhooks` — an unlisted route — as the regression case). `ROUTE_SCOPES` deliberately under-maps rather than over-maps (e.g. `apps:restart` covers only `POST .../restart`, not `start`/`stop`) — fails closed by construction, a documented follow-up rather than a gap. `scope_enforce` is mounted on `main_router` only, not `embed_router` (§6) — out of scope for a bypass that's entirely about `main_router` admin/API actions, per the task's own scope note.

**Capability tiers (part 2 of the same task):** `integrations::CAPABILITY_TIERS` (§2) gives `POST /api/integrations/tokens` an optional coarser `tier` shortcut (`read`/`deploy`/`exec`/`admin-never`) on top of the scope enforcement above; `admin-never` mints an empty scope set, which is blocked structurally by the same default-deny table rather than by trusting a self-reported label.

**What the PR history shows, for context:** the first attempt at this task (the one prior maps described as reviewed-sound-but-rejected) failed adversarial review because its commit also carried an unscoped, whole-crate `cargo fmt` reformat that cascaded into forbidden-zone files with no covering ADR. The version that actually merged (`f263b66` + `f595e3a` + `14b0da3`) is a from-scratch rewrite scoped correctly to ADR-003's granted paths, and the same-day `14b0da3` follow-up additionally hardened `router()` against the underlying `rustfmt`-cascade mechanism itself (`#[rustfmt::skip]`, §6) — though as the preamble notes, that hardening only protects `router()`'s own route table, not other files, from a future out-of-scope `cargo fmt --all`.

Also relevant, unchanged by this fix: `node_enroll.rs`'s heartbeat token (§2/§3) was deliberately designed **not** to be an `api_tokens`-table token, specifically to avoid this class of bug before it was fixed generally — see the doc comment at `node_enroll.rs:64-68`. That design choice remains sound defense-in-depth (a node heartbeat token still isn't scope-checked by `scope_enforce`, since it was never an `api_tokens` row in the first place) even though the underlying gap it was built around is now closed.

---

## 5. Frontend panel → API dependency map

Frontend root: `frontend/src/`. Pages live in `frontend/src/pages/*.tsx`; a parallel "AIOS" native-panel system lives in `frontend/src/aios/panels/*.tsx` for an alternate embedded-desktop UI shell. Both consume a single shared API client, `frontend/src/api/client.ts`, organized as one `export const api = {...}` object with per-domain namespaces each calling a fixed `/api/...` path via a shared `request()` helper that sends `credentials: 'include'` and throws `ApiClientError` on non-2xx.

**Two additional, independent client surfaces now exist alongside `frontend/`, both new since the prior map:**

- **`frontend/src-tauri/`** — a Tauri (Rust) desktop-app wrapper around the *same* `frontend/` React build, not a separate UI. `frontend/src/api/client.ts` gained an `isTauri()` helper (`client.ts:20-22`, checks `'__TAURI__' in window`) and routes requests through `@tauri-apps/plugin-http`'s fetch when running inside the Tauri webview, because the desktop app talks to a *remote* VoidTower instance from its own `tauri://` origin rather than same-origin (see the doc comment at `client.ts:9-18`). `App.tsx` and `Sidebar.tsx` both branch on `isTauri()`: the desktop app's index route shows a new simplified `HomePage` (`frontend/src/pages/Home.tsx`, new) instead of redirecting to `/dashboard`, and the sidebar gets a synthetic prepended "Home" nav entry plus relabels "Dashboard" to "Advanced."
- **`mobile/`** — a standalone Expo/React Native app (own `package.json`, not a workspace member of `frontend/`), with its own ported API client (`mobile/src/api/client.ts`, "ported from `frontend/src/api/client.ts`, same endpoint paths/response shapes") and types (`mobile/src/api/types.ts`). It consumes: `auth` (login/logout/me/password), `metrics.current`, `services.list`, `containers.list`, `apps.deployed`, and — the one place any frontend client consumes `node_enroll.rs`'s routes — `api.nodes.enroll` from `mobile/src/screens/NodeEnrollmentScreen.tsx`, for a user to redeem an admin-issued pairing code and join their device to the fleet.

**Member-role gating (new, `frontend/src/` only):** `App.tsx`'s `MemberGate` (`App.tsx:60-71`) redirects any `role === 'member'` session to `/` for every path except `/` and `/apps`; `Sidebar.tsx` separately hides all infra nav for `member` users, replacing it with a two-item "Home"/"My Apps" group (`Sidebar.tsx:432-443`) — both client-side conveniences, not a security boundary (the real enforcement is server-side: `members.rs`'s handlers and `apps.rs`'s member-branching, §1/§2).

Because every page/panel imports from this one client rather than hand-rolling fetch calls, the panel↔endpoint mapping is 1:1 with the domain namespaces in `client.ts` and the route table in §2:

| Panel/page group | API namespace used (client.ts) | Backend routes (§2) |
|---|---|---|
| `Login.tsx`, `Bootstrap.tsx` | `api.auth`, `api.oidc` | `/api/auth/*`, `/api/oidc/config` |
| `Home.tsx` (new) | `api.alerts`(implied, for its alert summary) | Desktop-app/`member`-role landing page — plain-language summary instead of the technical dashboard |
| `Containers.tsx`, `ContainerDetail.tsx`, aios `containers.tsx` | `api.containers` (implied) | `/api/containers/*` |
| `Services.tsx`, aios `services.tsx` | `api.services` | `/api/services/*` |
| `AppVault.tsx`, `DeployConfigModal.tsx`, `DeployToProxmoxModal.tsx` | `api.apps` | `/api/apps/*`; `AppVault.tsx` gained member-facing deploy UI (storage-drive/target-node override fields feeding `DeployRequest`'s new member-only fields, §2) |
| `Backups.tsx`, aios `backups.tsx` | `api.backups` | `/api/backups/*` |
| `Alerts.tsx`, aios `alerts.tsx` | `api.alerts` | `/api/alerts/*` |
| `Files.tsx`, aios `files.tsx` | `api.files` | `/api/files/*` |
| `AI.tsx`, `AiProviders.tsx`, `Studio.tsx`, aios `studio.tsx` | `api.ai`, `api.aiProviders`(implied), `api.studio`(implied) | `/api/ai/*`, `/api/studio/*` |
| `Models.tsx` | `api.models`(implied) | `/api/models/*`, `/v1/*` |
| `Proxies.tsx`, aios `proxies.tsx` | `api.proxy`(implied) | `/api/proxy/*` |
| `Security.tsx` | `api.security`(implied), plus a new `MfaPolicyPanel` (admin-only, direct `fetch` calls, not via the shared client) | `/api/security/*`, `/api/settings/mfa-policy` |
| `ForceTotpSetup.tsx` (new component) | `api.totp` | `/api/auth/totp/setup`,`/enable` — modal forcing TOTP enrollment on login when `PublicUser.mfa_required` is true (derived server-side from `settings::mfa_required_for_role`, not stored on the user row, §1) |
| `Terminal.tsx` | `api.terminal`(implied) | `/api/terminal/*` |
| `Audit.tsx`, `Timeline.tsx` | `api.audit`, `api.timeline`(implied) | `/api/audit`, `/api/timeline` |
| `Automation.tsx`, aios `automation.tsx` | `api.automation`(implied) | `/api/automation/*` |
| `CustomizationTabs.tsx`, `CustomTabView.tsx`, aios `tabs.tsx` | `api.tabs`(implied) | `/api/tabs/*` |
| `CustomizationNavigation.tsx` | `api.navConfig`(implied) | `/api/nav-config*` |
| `Firewall.tsx`, aios `firewall.tsx` | `api.firewall`(implied) | `/api/firewall*` |
| `Capabilities.tsx` | direct fetch of `/api/capabilities` | `/api/capabilities` |
| `Diagnostics.tsx` | `api.diagnostics`(implied) | `/api/diagnostics` |
| `Secrets.tsx`, aios `secrets.tsx` | `api.secrets`(implied) | `/api/secrets/*` |
| `Network.tsx`, aios `network.tsx` | `api.network`(implied) | `/api/network/neighbors` |
| `WireGuard.tsx`, aios `wireguard.tsx` | `api.wireguard`(implied) | `/api/wireguard*` — no UI screen yet calls `node_enroll.rs`'s admin routes (`mintPairingCode`/`list`/`remove`), though the mobile client declares wrappers for them, see §2 |
| `Settings.tsx` | `api.settings`(implied) | `/api/settings/*`; gained a new-role picker (`guest`/`demo`/`member`) and guest `expires_at` input in user creation, plus member access/quota/drive configuration UI wired to `api.members.*` |
| `VMs.tsx`, aios `vms.tsx` | `api.vms`(implied) | `/api/vms/*` |
| `Tags.tsx`, aios `tags.tsx` | `api.tags`(implied) | `/api/tags/*` |
| `Storage.tsx`, aios `storage.tsx` | `api.storage`(implied) | `/api/storage/*` |
| `Integrations.tsx`, aios `integrations.tsx` | `api.integrations`(implied) | `/api/integrations/*` |
| `Updates.tsx` | `api.updates`(implied) | `/api/updates/*` |
| `ProxmoxPage.tsx` | `api.proxmox`(implied) | `/api/proxmox/*` |
| `Mods.tsx` | `api.mods`(implied) | `/api/mods/*` |
| `Plugins.tsx`, `PluginPage.tsx` | `api.plugins`(implied) | `/api/plugins/*` |
| `LxcPage.tsx` | `api.lxc`(implied) | `/api/lxc/*` |
| `Policy.tsx` | `api.policy`(implied) | `/api/policy/*` |
| `Themes.tsx`, `CustomizationBranding.tsx` | `api.settings`/theme-specific calls | `/api/settings/*`, theme table (no dedicated route seen) |
| aios `agents.tsx`, `dashboard.tsx` | `api.agents`, metrics/dashboard aggregation | `/api/agents/*`, `/api/metrics/*` |
| — (no dedicated page) | `api.members` (`client.ts:78-102`: `list`, `myAccess`, `myNodes`, `access`, `grantAccess`, `revokeAccess`, `setCustomDeploy`, `setQuota`, `addDrive`, `removeDrive`) | `/api/members/*` — consumed from within `Settings.tsx`'s member-management section rather than a standalone page |

Note: the client-file architecture and the explicitly-new rows above were confirmed directly; the remaining "implied" rows follow the same one-namespace-per-backend-module convention visible throughout the file but weren't individually re-opened for this pass (unchanged from the prior map's caveat) — a task consuming this map for frontend work should grep `client.ts` for the exact namespace before assuming.

---

## 6. Observed code conventions

- **Auth pattern (repeated per-file, not shared middleware):** nearly every `api/*.rs` file defines its own local `require_user`/`require_admin`/`require_owner` async fn that reads the `vt_session` cookie from `CookieJar`, calls `auth::validate_session`, and maps `None`/error to `AppError::Unauthorized`/`Internal`. Role checks are inlined per-handler rather than expressed as a reusable Axum extractor. **The two new handler files (`members.rs`, `node_enroll.rs`) follow this exact same copy-per-file pattern** rather than introducing a shared helper — consistent with the existing convention, not a new one.
- **Global middleware layering:** `main_router` now has **four** cross-cutting concerns implemented as actual Axum middleware: `security_headers`, `demo_guard::middleware`, `scope_enforce::middleware` (**new, P0-06**), and `bearer_auth::middleware`, layered in that source order (`mod.rs:450-459`) — `.layer()` wraps outward, so the **last**-added layer is outermost and runs first on a request: `bearer_auth` (outermost/first) → `scope_enforce` → `demo_guard` → `security_headers` → `cors` (innermost, closest to the router). This ordering is load-bearing twice over: a bearer-token request has its temp session cookie *and* its `TokenScopes` extension attached by `bearer_auth` before `scope_enforce` ever runs (so there's something to check), and before `demo_guard` ever inspects the request for a `role = "demo"` session. `embed_router` (app-embed iframes / plugin assets) only ever gets `cors` + `bearer_auth` — no `scope_enforce`, `demo_guard`, or `security_headers` — unchanged from the prior map, and P0-06 deliberately did not extend `scope_enforce` to it (out of scope for a `main_router`-only bypass, per the task's own scope note in §4). CORS is permissive (`Any`/`Any`/`Any`).
- **`#[rustfmt::skip]` to protect a hand-aligned table from `rustfmt`'s `mod`-graph cascade (new, P0-06's `14b0da3` follow-up):** `router()` in `api/mod.rs` is now annotated `#[rustfmt::skip]` (`mod.rs:104`, with a comment explaining why, `mod.rs:97-103`) because `rustfmt`, invoked on this file (or transitively via `main.rs`, the crate root), treats every file reachable through `mod` declarations as one formatting unit and will silently reflow all of them — including the hand-aligned `.route("path", ...)` calls this file's own `risk_class::tests::every_registered_route_has_a_risk_class` test parses as text (§1, §4) and requires the path string to stay adjacent to `.route(`. This only protects `router()`'s own table from being reflowed as a side effect of formatting `api/mod.rs` correctly — it does not prevent an out-of-scope `cargo fmt --all` invocation from reformatting every other file in the crate, which is the actual, still-unresolved cause of the persistent 62-file uncommitted diff described in the preamble.
- **Error handling:** single `AppError` enum via `thiserror`, with `#[from]` conversions for `sqlx::Error` and `anyhow::Error`; handlers return `Result<Json<T>>` and propagate with `?` or explicit `.map_err(...)`.
- **DB access:** raw `sqlx::query`/`query_as::<_, T>` with `#[derive(sqlx::FromRow)]` structs defined inline in the handler file (not a separate repository/DAO layer) — the new `members.rs`/`node_enroll.rs` continue this (e.g. `NodeRow` in `node_enroll.rs:74-85`, `DriveSummary` in `members.rs:71-79`).
- **Audit logging:** mutating human-initiated actions call `audit::log(...)` (`backend/src/audit/mod.rs:20-52`, §1); AI/automation-sourced actions use the sibling `audit::log_sourced(...)` with an extra `source` param (e.g. `"odysseus"`). Every new `members.rs`/`node_enroll.rs` mutating handler follows this convention (e.g. `members.access.grant`, `nodes.enroll`, `nodes.pairing_code.create`).
- **Actor detection for policy/voidwatch:** the `MaybeTokenActor` Axum extractor (`policy.rs`) reads an `ApiTokenActor` marker inserted by `bearer_auth::middleware`; ingress handlers construct a `voidwatch::Actor{ kind }` explicitly per call site (`ActorKind::ApiToken` in `mcp.rs`'s bearer-token dispatch, `ActorKind::User` in `studio.rs`'s session-authenticated path, `ActorKind::Automation` in `integrations.rs`'s webhook paths) rather than deriving it generically — this is opt-in/explicit per-handler, not automatic, same spirit as the pre-existing `if is_token { policy::check(...) }` pattern in `containers.rs`/`services.rs` (those two files' direct `policy::check` calls are pre-existing and explicitly out of scope for the voidwatch migration, per `voidwatch/mod.rs`'s own doc comment).
- **Naming:** snake_case DB columns/Rust fields, kebab-case-ish REST paths, route path segments named after the resource (`:id`, `:name`, `:project_name`, `:host_id`, `:vmid`, `:user_id`, `:drive_id`, `:app_id` depending on domain).
- **Test conventions:** still sparse but growing. `#[test]`/`#[tokio::test]` now appear in `backend/src/api/tabs.rs`, `backend/src/api/agents.rs`, `backend/src/api/scope_bypass_tests.rs` (**new, P0-06** — a dedicated top-level test file, declared `#[cfg(test)] mod scope_bypass_tests;` in `api/mod.rs:94-95` rather than an inline `mod tests` block; seven named acceptance tests exercising the full router end-to-end via `tower::ServiceExt::oneshot`, requiring `tower`'s `util` feature added to `[dev-dependencies]` only), `backend/src/voidwatch/mod.rs` (17 tests including P0-03's mode-ladder acceptance suite, `voidwatch/mod.rs:249-934`), `backend/src/voidwatch/mode.rs` and `backend/src/voidwatch/risk_class.rs` (each their own test module, new in P0-03), `backend/src/voidwatch/allowlist_seed.rs`, `backend/src/policy.rs` (`#[cfg(test)] mod tests`, added alongside P0-02, including a data-driven "default verdict by actor class" table), `backend/src/api/integrations.rs` (tests for `run_automation_job`/`webhook`, extended by P0-03's `d378bb1` fix with an end-to-end `AllowRequireSnapshot`-blocks regression test), and `backend/src/api/mcp.rs`/`backend/src/api/redact.rs`/`backend/src/api/studio.rs` (P0.5's redaction corpus tests — a fixed set of known-secret-shaped strings asserted absent from tool-call/context-bundle output end-to-end). No established test-file-naming convention beyond `#[cfg(test)] mod tests { ... }` inline at the bottom of the same file as the code under test. A recurring pattern in the newer tests: since `db::run_migrations` doesn't create `policy_rules`/`voidwatch_default_allowlist`/`voidwatch_mode_settings` (those live in `db::init_pool`, outside the migration path tests use), test setup functions create all three directly with inline `CREATE TABLE` statements — `voidwatch::tests::create_policy_tables` (`voidwatch/mod.rs:259-305`) is `pub(crate)` specifically so `voidwatch::mode`'s own test module can reuse it rather than duplicating the statements. Also: `backend/tests/schema_golden.sql` plus a `db/mod.rs` test (`schema_golden_file_matches_live_schema_after_migration`, `db/mod.rs:752-`) that diffs live `sqlite_master` schema for both voidwatch tables against it, and a sibling test (`pre_existing_tables_byte_identical_after_upgrade`, `db/mod.rs:812-`) proving `init_pool` adds new tables to a simulated pre-upgrade database without dropping/narrowing anything pre-existing — a heavier-weight regression style than anything in the codebase before P0-02, worth following for future schema-adjacent grants.
- **Parser-based exhaustiveness tests (new pattern, P0-03):** two source-grep-style regression tests now go further than string-matching — `risk_class::tests::every_registered_route_has_a_risk_class` (`risk_class.rs:677-717`) parses `api/mod.rs` with a manual balanced-paren scanner to extract every registered `(method, path)` and asserts a bijection against `ROUTE_RISK_CLASSES`, and `voidwatch::mod::tests::evaluate_is_the_only_caller_of_policy_check_from_ai_ingress` (`voidwatch/mod.rs:533-549`) is the substring-based precedent it extends. Same spirit as `db/mod.rs`'s schema-golden test: catch a class of drift (unclassified route, ungated ingress point, schema divergence) at build/test time rather than relying on a human noticing in review.
- **Shared-reasoning-helper-to-prevent-drift (new pattern, established by the `d378bb1` bugfix):** `integrations.rs`'s `verdict_block_reason()` (`integrations.rs:726-735`) exists because two near-identical inline `match` arms at separate call sites drifted apart — one was correct for its fixed resource type, the other silently inherited wrong reasoning via copy-paste. The fix was to make both call sites share one function rather than just fixing the one that was wrong. Worth applying this pattern proactively anywhere near-identical verdict/permission-handling logic is about to be copy-pasted to a second call site.
- **`test_support.rs` as a per-module shared-fixture file (new, P0.5):** `api/mcp.rs` declares `pub(crate) mod test_support;` (backed by `api/test_support.rs`, `#![cfg(test)]`-gated) providing a shared `AppState` builder (`build()`) and session/DB setup helpers (`setup_db()`, `user_with_session()`) so `redact.rs`'s and `mcp.rs`'s own test modules don't each hand-roll `AppState` construction. First instance of a dedicated fixture file in this codebase — prior test modules each inlined their own setup helpers locally (e.g. `voidwatch::tests::setup_db` above is still local, not factored out this way) — worth reusing rather than re-duplicating if a future task's tests need the same `AppState` boilerplate.
- **Secrets:** encrypted with AES-256-GCM using a 32-byte key generated/stored at `<config_dir>/secrets.key` (0600 perms); the same decrypt routine is duplicated in `api/proxmox.rs` rather than shared — unchanged, still a candidate for consolidation but not something to change without a task naming it.
- **CLI subcommands** (`main.rs`) exist in parallel to the HTTP API for `user` and `backup` management, sharing the same `backend::auth`/`backend::backups` functions as the web handlers.
- **Core-logic-vs-HTTP-handler split, newly established by this batch:** `wireguard.rs`'s `add_peer`/`delete_peer` handlers were split into thin HTTP wrappers around new `pub(crate) add_peer_core`/`delete_peer_core` functions specifically so `node_enroll.rs` could reuse the exact provisioning logic without duplicating it or going through an HTTP round-trip. This is the first instance of that split pattern in the codebase (every other cross-handler reuse, e.g. `mcp.rs`/`studio.rs` sharing `invoke_tool`, was already function-level, not handler-level) — worth following if a future task needs one handler's logic from another.
- **Frontend:** one shared typed API client per client surface (`frontend/src/api/client.ts`, `mobile/src/api/client.ts` as an explicit port of it) namespaced by backend domain, `credentials: 'include'` (web) on every request; `frontend/`'s client additionally branches on `isTauri()` to use `@tauri-apps/plugin-http`'s fetch instead of the browser's when running inside the desktop wrapper (§5). Two parallel UI shells exist within `frontend/` (`frontend/src/pages/*` classic pages and `frontend/src/aios/panels/*`), plus now two additional, independent client trees (`frontend/src-tauri/`, `mobile/`) that are thin/native wrappers rather than forks of the React app — new panel work should check which shell *and* which client tree the task actually targets before choosing a template.
