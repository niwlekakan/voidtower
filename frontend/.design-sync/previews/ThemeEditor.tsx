import { ThemeEditor } from 'voidtower-frontend'

export function Default() {
  return (
    <div style={{ width: 760, background: 'var(--bg-panel)', padding: 20, borderRadius: 8 }}>
      <ThemeEditor />
    </div>
  )
}
